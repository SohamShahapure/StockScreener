# Baked into the Vercel production build (next build reads this file).
# This is the live Render backend URL - not a secret, safe to commit.
# If you set NEXT_PUBLIC_API_URL in the Vercel dashboard too, make it match
# this value (an already-set env var takes precedence over this file).
NEXT_PUBLIC_API_URL=https://stockscreener-api-safu.onrender.com
